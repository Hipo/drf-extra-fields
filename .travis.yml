language: python

env:
    - TESTENV=py27
    - TESTENV=py34

before_install:
    - sudo apt install libgdal-dev

install:
    - pip install tox

script:
    - tox -e $TESTENV

notifications:
    email: false
